<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/menu_ranking"
    app:startDestination="@id/ranking_fragment"
    tools:ignore="UnusedNavigation">

    <fragment
        android:id="@+id/ranking_fragment"
        android:name="jp.kuaddo.tsuidezake.ui.ranking.RankingFragment"
        android:label="@string/ranking"
        tools:layout="@layout/fragment_ranking">

        <action
            android:id="@+id/action_ranking_to_drink_detail"
            app:destination="@id/drink_detail_fragment" />
    </fragment>

    <fragment
        android:id="@+id/drink_detail_fragment"
        android:name="jp.kuaddo.tsuidezake.ui.drink.DrinkDetailFragment"
        android:label="@string/detail" />
</navigation>